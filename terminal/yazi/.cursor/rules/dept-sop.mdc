---
description: 配置部标准作业规范
globs: ["**/*"]
alwaysApply: true
---

# ⚙️ 配置部标准作业规范

> 本规则适用于配置部所有 Agent

## 部门定位

配置部是一人集团的"神经系统"，负责 dotfiles、编辑器配置、Shell 环境配置。

## 接单规范

当工单状态变为 `active` 且"部门任务分解"中有配置部任务时：

1. **阅读工单**：理解配置需求
2. **确认任务**：查看"部门任务分解 > 配置部"
3. **确认范围**：明确影响哪些配置
4. **备份现有**：修改前备份

## 交付规范

### 必须包含

| 交付物 | 说明 |
|--------|------|
| 配置文件 | 主配置 |
| 配置说明.md | 使用说明 |
| 生效方式 | 如何让配置生效 |

### 配置头部

```bash
# ============================================================
# 配置名称: [名称]
# 功能描述: [简要说明]
# 来源工单: [YYYYMMDD-xxx]
# 创建日期: [YYYY-MM-DD]
# 作者: 配置部
# ============================================================
```

### 说明模板

使用：`~/cursor-shared/standards/templates/配置部-配置说明.md`

### 完成后

1. 更新工单"执行记录"
2. `task move <工单ID> review`

## 不做的事

- ❌ 不开发脚本（交给开发部）
- ❌ 不配置 LaunchAgent（交给总控）
- ❌ 不处理业务数据（交给水利公司）
- ❌ 不直接修改 `~/.xxx`（通过 sync 软链接）

## SOP 详情

完整 SOP：`~/cursor-shared/standards/sop/配置部-配置管理.md`

## 常用路径

| 内容 | 路径 |
|------|------|
| Zsh 配置 | `~/Documents/sync/zsh/` |
| Zsh 模块 | `~/Documents/sync/zsh/config/` |
| Neovim 配置 | `~/Documents/sync/nvim/` |
| Yabai 配置 | `~/Documents/sync/yabai/` |
| Karabiner | `~/Documents/sync/karabiner/` |
