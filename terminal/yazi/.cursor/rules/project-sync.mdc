---
description: 配置部项目上下文 - dotfiles 管理与 Agent 分发
globs: ["**/*"]
alwaysApply: true
---

# 配置部项目上下文

> 📍 项目：`~/Documents/sync/`  
> 🎯 定位：一人集团 **配置部**  
> 🔧 职责：dotfiles、Neovim、Zsh、Yabai、Karabiner 等开发环境配置

---

## 📁 项目结构

```
~/Documents/sync/（配置部）
│
├── 🖥️ nvim/           # Neovim 配置
│   ├── init.lua       # 入口文件
│   ├── lua/config/    # 核心配置
│   └── lua/plugin/    # 自定义插件
│
├── 🐚 zsh/            # Zsh 配置
│   ├── zshrc          # 主配置
│   ├── zimrc          # Zim 框架
│   └── config/        # 模块化配置
│
├── 🪟 yabai/          # Yabai 窗口管理
│   ├── config/yabairc # 主配置
│   └── config/skhd/   # skhd 快捷键
│
├── ⌨️ karabiner/      # 键盘映射
│   ├── karabiner.json # 主配置
│   └── assets/        # 规则模块
│
├── 🔍 fzf/            # FZF 配置
├── 📦 git/            # Git 配置
├── 📱 IDE/            # IDE 配置
└── 📦 npm/            # NPM 配置
```

---

## 🤖 Agent 分发规则

本项目使用 **共享 Agent 分发模式**，根据任务类型自动路由到合适的专业 Agent。

### Agent 分发表

| 配置类型 | 关键词 | 分发到 |
|---------|-------|-------|
| **Neovim** | nvim, lua, 插件, LSP, 补全 | `@~/cursor-shared/agents/L1_global/nvim_master.md` |
| **终端环境** | zsh, shell, 别名, fzf, 终端 | `@~/cursor-shared/agents/L1_global/automation_engineer.md` |
| **窗口管理** | yabai, skhd, 窗口, 平铺 | `@~/cursor-shared/agents/L1_global/automation_engineer.md` |
| **键盘映射** | karabiner, 按键, hyper | `@~/cursor-shared/agents/L1_global/automation_engineer.md` |
| **脚本开发** | python, 脚本, 工具 | `@~/cursor-shared/agents/L1_global/script_engineer.md` |
| **Raycast** | raycast, 快捷命令 | `@~/cursor-shared/agents/L1_global/raycast_developer.md` |

### 项目架构师

复杂或跨模块需求，调用项目架构师进行协调：

```
@agents/00_config_architect.md 帮我分析这个需求应该怎么处理
```

---

## 🔧 快速调用

### Neovim 配置

```
@~/cursor-shared/agents/L1_global/nvim_master.md 

任务：优化 ~/Documents/sync/nvim 的 LSP 配置
```

### Zsh 配置

```
@~/cursor-shared/agents/L1_global/automation_engineer.md

任务：在 ~/Documents/sync/zsh/config/mappings.zsh 添加新快捷键
```

### Yabai 配置

```
@~/cursor-shared/agents/L1_global/automation_engineer.md

任务：修改 ~/Documents/sync/yabai/config/yabairc 窗口布局规则
```

---

## 📜 关联规则

本项目已链接以下共享规则：

| 规则文件 | 描述 | 适用 |
|---------|------|------|
| `shared-nvim.mdc` | Neovim Lua 配置规范 | `*.lua`, `nvim/**` |
| `shared-python.mdc` | Python 编码规范 | `*.py` |
| `shared-shell.mdc` | Shell 脚本规范 | `*.sh`, `*.zsh` |
| `shared-raycast.mdc` | Raycast 脚本规范 | raycast 脚本 |

---

## 🏠 关键路径

```bash
# 配置部根目录
SYNC_DIR="$HOME/Documents/sync"

# 各模块配置
NVIM_DIR="$SYNC_DIR/nvim"
ZSH_DIR="$SYNC_DIR/zsh"
YABAI_DIR="$SYNC_DIR/yabai"
KARABINER_DIR="$SYNC_DIR/karabiner"

# 实际链接位置（部署后）
~/.config/nvim → $NVIM_DIR
~/.zshrc → $ZSH_DIR/zshrc
~/.config/yabai → $YABAI_DIR/config
~/.config/karabiner → $KARABINER_DIR

# 共享资源
SHARED_DIR="$HOME/cursor-shared"
AGENTS_DIR="$SHARED_DIR/agents"
RULES_DIR="$SHARED_DIR/rules"
```

---

## 📝 编辑规范

### 通用原则

1. **模块化** - 配置按功能拆分到独立文件
2. **可注释** - 关键配置添加中文注释
3. **可回滚** - 重大修改前备份原配置
4. **可测试** - 修改后验证功能正常

### 文件命名

| 类型 | 命名格式 | 示例 |
|------|---------|------|
| 主配置 | 小写无后缀/标准名 | `zshrc`, `init.lua` |
| 模块配置 | `功能名.扩展名` | `keymaps.lua`, `modern-tools.zsh` |
| 脚本工具 | `动词_名词.sh` | `toggle_window.sh` |

### 提交规范

```bash
# 格式
<type>(<scope>): <description>

# 示例
feat(nvim): 添加 Python LSP 支持
fix(zsh): 修复 FZF 预览乱码
refactor(yabai): 重构窗口移动脚本
docs(karabiner): 更新键位映射说明
```

---

## 🔗 集团资源

| 部门 | 路径 | 职责 |
|------|------|------|
| 📍 总控中心 | `~/cursor-shared/` | 共享 Agent、规则、记忆 |
| 🔧 开发部 | `~/useful_scripts/` | 脚本、工具、自动化 |
| ⚙️ 配置部 | `~/Documents/sync/` | 本项目 |
| 🌊 水利公司 | `~/Downloads/zdwp/` | 业务项目 |

---

## 🔄 版本记录

| 版本 | 日期 | 更新 |
|------|------|------|
| v1.0 | 2025-12-15 | 初始版本，配置部上下文与 Agent 分发 |
